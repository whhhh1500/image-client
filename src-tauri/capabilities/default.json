{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "opener:allow-open-path",
      "allow": [
        { "path": "$HOME/ImageClient/logs" },
        { "path": "$HOME/ImageClient/logs/**" }
      ]
    },
    "opener:allow-reveal-item-in-dir",
    "dialog:allow-open",
    "dialog:allow-message"
  ]
}
