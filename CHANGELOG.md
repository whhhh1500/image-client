# 版本记录

本项目遵循语义化版本号。日期使用 `YYYY-MM-DD`。

## 0.2.4 - 2026-09-09

- 修复保存或拼接大视频时界面卡死/内存暴涨：字节数组不再被日志脱敏逐字节枚举，跨 IPC 传输改用 base64；数据库初始化失败后可以重试，不再让整个数据层永久失效。
- 修复资产库里保存资源信息会抹掉漫画页面血缘字段（作品、章节、生成运行、清单指纹），导致后续对账失败的问题。
- 修复视频工作区保存后仍显示「草稿未保存」并阻断下游 AI 优化，以及项目快速切换时生成表单串用上一个项目参数的问题。
- 修复视频任务重试会重新生成已成功镜头（重复计费）的问题，现在按原镜头组续跑，并显示失败原因。
- 修复分镜 Markdown 中多出的三级标题会让整份分镜解析失败的问题；文本镜头的锚点不再被误判为无效参考资产。
- 图像/视频/漫画的耗时命令不再占用界面线程：压缩、格式转换、外部文件导入、媒体保存等改为后台线程执行，导入大文件时不再阻塞数据库。
- 图片解码增加像素与内存上限，防止超大尺寸图片导致进程直接退出；大响应体改为边收边校验大小。
- 安全：`db_select`/`db_execute` 改用 SQLite authorizer 硬约束表白名单；媒体托管令牌迁出渲染进程可读的表；更改接口地址时必须同时提交新 Key，避免已保存的密钥被发往新地址。
- 性能：资产与任务历史新增 `created_at` 索引；漫画目录按作品一次性加载；参考图与导出文件哈希按修改时间缓存；HTTP 连接池进程内复用；release 构建开启 LTO、单 codegen unit 与 strip；生图/视频面板与模板库改为按需渲染与视口加载。

## 0.2.3 - 2026-09-08

- 小说原文改为由共享资产管理器统一新建、编辑和发布章节修订；每次保存均发布当前项目可复用的小说文本资产。漫画与短剧分别消费该资产，不再混用工作区状态。
- 短剧引用小说时固定采用带修订号的章节快照；小说出现新修订不会静默改写已开始的短剧，用户可显式采用最新章节修订。
- 资产库按 AI 生图、AI 视频、小说、小说漫画、短剧和本地上传分类与分组；图像、视频工作区可从资产库或外部文件导入素材，并在预览后确认提示词、合并提示词或实际参考用途。
- 视频逐镜参考支持本地图片随生成请求发送、显式托管为公网 URL、漫画页图片与已有 HTTPS URL；本地视频继续走媒体托管。逐镜计数、首帧选择、移除操作和历史恢复均保留两类图片引用及其来源。
- 漫画保存章节级 Prompt 注入、单页重画注入和实际提交的完整 Prompt 快照；漫画目录可区分页 Prompt Markdown 与真实 Provider 请求，便于复核。
- 发布提供 Windows x64 portable.exe、macOS arm64 app.tar.gz 和 Linux x64 AppImage；不提供 setup、DMG、DEB 或 Intel Mac 重复安装包。

## 0.2.2 - 2026-09-07

- 修复短剧工作台无法导入视频的问题：可直接上传本地 MP4、WebM、MOV，或从当前项目视频资产中选择作品参考。
- 导入的视频作品按短剧工作区隔离，并将资源名称、模型、时长和关联信息提供给导演、编剧、视频锚点、视频分镜与 QC Agent；不会把视频内容伪装成小说事实。
- 视频生成页的“导入创作参考”新增视频类型；公网 HTTPS 视频可进入当前镜头的 zzone `videos[]`，本地视频保留为创作参考与溯源，并明确提示 Provider 只接受公网 HTTPS URL。

## 0.2.1 - 2026-09-07

- 小说漫画新增作品级多图画风参考：可直接上传或从当前项目图片资源选择最多 8 张作品图，并与视频资源、其他项目和其他小说严格隔离。
- 支持使用当前配置的多模态文本模型从参考图提取可编辑的 Markdown 视觉宪法；视觉宪法会统一进入作品设定、剧本、分页分镜、页 Prompt、AI 优化和最终漫画生图。
- 漫画生图通过 Provider `references[]` 真实传递作品画风图，并在每张结果中冻结视觉宪法版本和参考图快照；修改画风后旧图只标记需更新，不自动重画或产生费用。
- 状态栏中的“ZZone 网关”改为可点击入口，使用系统默认浏览器打开网关邀请页面。

## 0.2.0 - 2026-09-07

- 重构视频生成工作区：按模型能力切换文本、首帧和多素材模式；桌面工作区当前只开放图片/视频参考并在提交前校验时长、清晰度、画幅、素材数量和公网 URL，声音与音频参考暂不提供。
- 视频模型目录在配置 API 后以当前 Key 的 `/v1/models` 为准；鉴权、网络或空目录错误明确展示。无可靠能力定义的新模型禁止盲目提交；MiniMax H3 2K/4K 别名已适配。
- 对齐 zzone 视频协议：首帧模式不额外发送画幅；Drama V2/Fast 总参考素材最多 12 项；轮询与下载瞬时失败保持同一 task ID 重试；task ID 写入资产溯源。
- 图像与视频工作区的生成状态、历史和资产入口进一步隔离。
- 视频生成改为逐镜执行：每镜使用自己的 Prompt 和模型支持时长，独立请求并保存为独立资源；生成后不自动拼接，用户可勾选至少两个镜头手动执行本地无损拼接。声音与字幕功能暂不提供。
- 短剧视频分镜统一为固定标题结构的 Markdown，记录时长、起始/动作/结束状态、承接镜头、画风/角色/场景/道具锚及参考策略。
- Agent 固定流水线调整为“导演 → 编剧 → 一致性 → 视频分镜 → 质检”；分镜可一键发送到视频工作区并保留来源文档溯源。
- 删除废弃的阶段模块、JSON 分镜格式和分镜逐镜出图入口。
- 视频分镜 Prompt 参考本地视频提示词资料，强化“主体 + 场景 + 运动”、单一主运镜、速度/幅度/结果、时间线和适量防崩约束。
- 短剧 Agent 改为分阶段 Markdown 工作台：共享原始资料、改编规划、剧本、视频锚点、视频分镜、质检和视频结果。
- 小说漫画保存或读取章节时发布共享 text/novel 资源；图像与视频工作区均可引用，媒体产物仍按类型隔离。
- 新增 `GET/PUT /api/v1/system/llm` 专用接口，自动化可只更新文本 Agent 的 URL、Key 和模型。
- 顶部改为独立的“图像生成”和“视频生成”导航，点击时一定进入对应工作区，移除重复模式切换。
- 小说漫画与短剧视频的“AI 优化”改为工作区级联：读取当前章节/工作区全部已保存文字产物和媒体关联元数据，按依赖顺序直接更新当前及已有下游文字产物为新版本；成功后不再只留下“需更新”标记，媒体仍需用户主动重画或重生成。
- GitHub 发布流水线改为 Linux 全量测试与四平台打包并行；主分支只跑一遍全量测试，四个原生任务仅编译 release profile 并分别预热稳定 target 缓存，标签构建只读复用；验证与所有安装包完成后再一次性创建 Release。
- 新增独立漫画画风参考实验脚本，可用多张作品图与小说原文提取视觉宪法、生成四类文字产物、执行一致性审计并可选生成一张真实参考图样页；支持断点续跑，不写入产品数据库。

## 0.1.1 - 2026-09-06

- 小说漫画生成、联动同步、文档优化和单页重画统一加入应用级合规规则：自动虚构化现实名称与商标，在保留画面张力的同时禁止血腥细节，并在无法安全改写时停止保存结果。

## 0.1.0 - 2026-09-06

- 提供图像、视频、小说漫画和通用短剧流水线工作区。
- 支持本地项目、资产、历史记录与多供应商配置。
- 新增右下角“使用说明”和“版本记录”入口。
- 换用 Image-Client 自有应用图标，移除 Tauri/Vite 默认品牌图标。
- 增加 Windows、macOS、Linux 原生验证与 GitHub Release 自动构建。
